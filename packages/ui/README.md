# @ds/ui

React component library for the design system, consuming `@ds/tokens`.

Components (Button, Input, Badge, Avatar, Spinner, Checkbox, Switch, Card, …)
are built here in the component/Storybook step, mapped to their Figma
counterparts via Code Connect.
