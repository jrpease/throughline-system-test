{
  "name": "@ds/tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens synced from Figma (CSS / Tailwind / TypeScript).",
  "files": ["dist"],
  "scripts": {
    "build": "echo \"Token build is configured in the token-sync step\" && exit 0"
  }
}
