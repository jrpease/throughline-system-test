# @ds/tokens

Design tokens for the design system, synced from Figma variables.

This package is populated by the **token-sync** step, which extracts the Figma
variables and runs them through Style Dictionary to emit code outputs
(CSS custom properties, a Tailwind preset, TypeScript constants, etc.).

> Source of truth: the Figma file. Do not hand-edit generated token files.
