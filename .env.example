# Copy this file to ".env" and fill in real values.
# IMPORTANT: .env is gitignored — never commit real secret values.
# This file (.env.example) holds only KEY NAMES as a template and is safe to commit.

# Chromatic visual-testing token — added later in the Storybook/Chromatic step.
CHROMATIC_PROJECT_TOKEN=
