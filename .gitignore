# Dependencies
node_modules/

# Build output
dist/
.turbo/
storybook-static/
# Note: packages/tokens/build/ holds generated token artifacts — intentionally tracked.

# Secrets — never commit real env values
.env
.env.local
.env.*.local

# Logs & OS noise
*.log
.DS_Store
coverage/

# Local tooling / editor session data
.claude/
